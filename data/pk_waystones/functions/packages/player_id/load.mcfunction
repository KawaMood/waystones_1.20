#> pk_waystones:packages/player_id/load
# Set an unique id for the player
# This package only sets a unique id per player once, no matter it is embed in other datapacks, if the player already has an id, it won't be redefined

scoreboard objectives add pk.player.id dummy